#自己写一个方法来实现日期时间格式的判断,目前使用正则表达式来判断此格式,有局限性(比如必须要有时间部分)

#excel导入程序,是否应该做成一个接口,然后AbstractExcelParser<T>再实现这个接口,再多想想


昨天做到调试datagrid,最新版本ie好象不支持,再调试看看

# 导入后显示错误的datagrid再优化一下
# 对上传的文件格式进行限制,json中反馈
# 查询后保存


# 调试的时候,excel文件会被占用,看看是什么问题
# 目前用的上传组件必须在个iframe中,所以我在main.jsp的addTab方法中要加一层iframe,不加这层iframe可以么?
# 在chrome中insu一用就跳到过期页面
# 在做查询的时候,查询条件要用Map,是否可以用一个类来代替这个Map,然后在service层把这个类转化为Map,持久层还是用Map作为参数

#核销,结算前,再次确定保单号和金额
#点击核销,结算后,要遮蔽
#排序后,选择的记录要重新选
#alert提示全改成模式对话框